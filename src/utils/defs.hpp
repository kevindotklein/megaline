#pragma once
#include <raylib.h>

namespace megaline {
  namespace utils {
    const Color red = ColorFromHSV(0.f, 0.8f, 1.f);
    const Color green = ColorFromHSV(90.f, 0.8f, 1.f);
    const Color blue = ColorFromHSV(220.f, 0.8f, 1.f);

    const float timeStep = 1.f;
  }
}